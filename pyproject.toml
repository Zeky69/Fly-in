[project]
name = "a-maze-ing"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "pygame>=2.6.1",
    "rich>=14.3.3",
    "typing>=3.10.0.0",
]

[dependency-groups]
dev = [
    "black>=24.8.0",
    "flake8",
    "mypy",
]
