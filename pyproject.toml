[project]
name = "a-maze-ing"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.6.1",
    "rich>=14.3.3",
]

[dependency-groups]
dev = [
    "black>=24.8.0",
    "flake8",
    "mypy",
]

[tool.mypy]
explicit_package_bases = true
